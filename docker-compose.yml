version: '3.0'

services:
  db:
    image: postgres

  web:
    build:
      context: .
      args:
        WORK_DIR: $WORK_DIR
    restart: always
    command: $WORK_DIR/prepare.sh
    volumes:
      - .:$WORK_DIR
    expose:
      - "8050"
    links:
      - db
      - redis
      - solr
#      - node

  nginx:
    restart: always
    build: ./config/nginx
    ports:
      - "805:8050"
    volumes:
      - .:$WORK_DIR
    depends_on:
      - web

  redis:
    image: redis
    ports:
      - "6379"

  solr:
    image: cpilsworth/solr
    ports:
      - "8983"

#  node:
#    build: ./config/node
#    volumes:
#      - .:$WORK_DIR
